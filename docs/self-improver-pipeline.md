# Self-improver pipeline (extracted from the root brain 2026-09-03, budget-pressure doctrine)

Active pointer: root `CLAUDE.md` § Connections links here. Read before touching the analyzer/drafts/validator/code-fixer chain, its gates, or its audit trail.

- **Archive → Analyzer → Drafts → Autonomous Apply**: `analyzer.ts`/`failure-analyzer.ts`/`feedback-analyzer.ts` produce `DraftProposal` objects (`drafts.ts`). **Fourth input since 2026-08-23: the 7-day alert census** (`lib/alert-census.ts`, built in-process nightly) — `censusProposals` routes deterministically BEFORE any LLM call: `deterministic-defect` families → code-fix drafts (cmd skills AND `target_kind:'maintenance-job'` jobs, verified by the scoped gate, rolled back from the ledger), `human-gated` → "Operator action needed" report section, `repeat-unchanged` → "Alert hygiene" section, exit-0 masked failures → "Masked failures"; the census headline prints even when there are 0 proposals (the week of 08-16 reported "nothing" under 110 alerts/day). Fully autonomous since 2026-07-11: `validator.ts`'s `isCriticalChange`/`hasRealSideEffects` no longer block — `gateAndApprove` records them as risk flags (`critical-skill`, `declares-secrets`) on applied change instead. Only `isProtected()` (`PROTECTED_SKILLS` — widened 2026-08-17 to whole git-workflow family) and validation floor (fix/new-skill failing validation stays `pending`, never deploys broken) still gate a proposal. Every terminal decision logged to `~/.pa/self-improver-audit.jsonl`; `pa improvements [--since N]` recomputes before/after eval. Thrash control: duplicate-pending skip, 3-day per-target cooldown, 14-day stale-draft sweep. **Code fixes too** (`pa/src/code-fixer.ts`): cmd-target proposals route to coding worker under floors F1–F6 (protected-diff inspection, test-integrity guard, same-run build+suite+bot-health verification with hard revert, clean-worktree precondition, per-run bounds — one attempt per target skill + disjoint files across a run's applied fixes + wall-clock budget (`PA_SELF_IMPROVER_CODE_FIX_BUDGET_MS`, default 40 min; optional `PA_SELF_IMPROVER_MAX_CODE_FIXES`) which replaced the global one-fix-per-night cap on 2026-08-23 (internal design record), PRIVATE-origin-only push); each fix is one commit, rolled back via `git revert` on regression. **Consolidation audit trail** (2026-08-18): `~/.pa/consolidation-audit.jsonl` records all memory-consolidation decisions (added/superseded/conflict/skipped). Spec: the 2026-08-18 memory-consolidation design record.
