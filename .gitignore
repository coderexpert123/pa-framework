# Dependencies
node_modules/

# Build output
**/dist/

# Environment & secrets
.env
.env.*

# OS
.DS_Store

# Debug logs
npm-debug.log*

# TypeScript
*.tsbuildinfo

# Deprecated (archived, don't track changes)
_deprecated/

# Claude Code
.claude/settings.local.json

# Python
__pycache__/
**/venv/
**/.venv/

# Generated data files
*.db
*.csv
*.pdf
*.log
# Atomic-write crash artifacts (learn_agent.py mkstemp tmps) + the regenerable
# ecosystem-kb mining watermark.
pa/data/*.tmp
pa/data/profile-history-archive.kb-watermark.json
# AI-089 (2026-07-27): profile.json + its eviction archive now live OUTSIDE the
# repo at ~/.pa/data/ (durability via the weekly secrets-backup skill, not git).
# These patterns stop accidental re-tracking if a stale PA_PROFILE_PATH override
# ever recreates them here.
pa/data/profile.json
pa/data/profile-history-archive.jsonl

# Secrets
**/credentials.json
**/google-token.json
**/secrets.env

# Editor
.obsidian/

# Root repo hygiene scratch/state
/.codex/
/pa/blackboard.json
# Generated reports at root (brain-recheck / audits — regenerated, not source)
/*_report.md
/brain_recheck_report.md

# === Repo-root hygiene: nothing personal/ephemeral lives at the root ===
# See docs/CONVENTIONS.md (public) + CLAUDE.md "Repository hygiene" (private).
# Repo root is reserved for repo-level files only: README, LICENSE, CLAUDE.md,
# optional MEMORY.md, BACKLOG.md, DEBUGGING.md, AGENTS.md/GEMINI.md aliases,
# .gitignore*, git-public.*. Anything else gets caught by the patterns below.

# External imports at root (PDFs, images, binaries — likely Telegram attachments)
/*.jpg
/*.jpeg
/*.png
/*.gif
/*.pdf
/*.db
/*.csv
/*.xlsx
/*.docx

# Email/conversation exports at root
/*_email_*.txt
/*_export_*.*
/*turns*.json
/*turns*.jsonl
/*turns*.txt
/extracted_facts.md

# Root is sparse — no code lives here (see CLAUDE.md hygiene). Ignore ALL
# root-level Python so ad-hoc / personal-data scripts can't be committed by
# accident — this closes the analysis.py-class gap that prefix patterns
# (/analyze_*.py, /check_*.py, ...) silently missed. Real scripts live in
# pa/scripts/ or projects/*/scripts/; throwaways go in scratch/.
/*.py
/check*.ps1
/scratch_*
# Worker/agent artifact: coding CLIs occasionally create this brain-bootstrap
# trigger file (referenced only in the global ~/.claude/CLAUDE.md protocol) in
# subdirectories they work in — recurred 3x during 2026-07 agent sessions.
**/.claude-needs-brain
/all_files.txt
/out*.txt
*.bak

# Misplaced Ecosystem KB schema files at repo root (canonical home is in your
# Obsidian vault / KB directory outside this repo, configured per-deployment).
# These are the standard filenames used by the `ecosystem-kb` skill.
/Action\ Items.md
/Preferences.md
/Financial.md
/People\ &\ Contacts.md
/Health\ &\ Medical.md

# LLM-agentic scratch writes: a worker occasionally "goes agentic" and writes
# its response/analysis to a local file at cwd instead of returning it as
# text or using its proper output path (run_brief.py has a documented retry
# for this exact failure mode). No application code writes these filenames —
# confirmed via full-repo grep 2026-07-09. If a new one recurs, add it here.
# /output.md added 2026-07-21: a full BRIEFING_START daily-mail-brief landed at
# root under this new name. /error_log.txt is the same class from the error
# side — a worker dumping a one-line provider error instead of returning it.
/message_to_user.md
/output.json
/output.md
/skill_proposals.json
/error_log.txt

# Scratch workspace — for ad-hoc / WIP / throwaway files (see scratch/README.md)
/scratch/*
!/scratch/.gitkeep
!/scratch/README.md

# Inside any project: scratch + temp/debug-prefixed artifacts
**/scratch/
**/temp_*.py
**/temp_*.json
**/temp_*.md
**/temp_*.txt
**/debug_*.py
**/debug_*.log
**/*-debug.log
**/test_repro.*
**/test_real_login.*

# Large input data dirs
src_pdfs/

# Per-project data dirs — raw inputs, processed outputs, exports. Regenerated
# from source and often personal/sensitive (e.g. financial statements); must
# stay out of version control. config/ under data/ is intentionally NOT ignored.
**/data/raw/
**/data/processed/
**/data/exports/
**/data/prompt_processed/
# Bulk extracts / caches / generated outputs (large + personal; regenerated)
**/data/openai_history/
**/data/full_openai_extract/
**/data/files_zip_extract/
**/data/dalle_zip_extract/
**/data/simulations/
**/data/legacy_output/
**/data/query-results/
**/data/market_cache/

# Send-receipt logs for send-or-silent skills (a watchdog cross-checks
# "should have sent" against these). Machine-appended runtime state, not source:
# only the CURRENT window is ever read, so history has no durable value, while
# tracking it would dirty the tree on every send (and trip the code-fixer's
# clean-worktree floor). Decision 2026-07-21; note the
# **/data/{raw,processed,exports}/ globs do NOT cover this path.
**/data/alerts_sent.jsonl

# Daily-mail-brief runtime artifacts (fetched email payloads + regenerated
# brief inputs/outputs — personal, recreated each run)
projects/daily-mail-brief/emails.json
projects/daily-mail-brief/analysis_input.md
projects/daily-mail-brief/briefing_output.md

# === Public-repo coexistence ===
# Separate .git directory + wrapper for the public framework repo
/.git-public/
/.gitignore-public
/git-public.cmd
/git-public.ps1

# Public-only files (untracked by private repo to avoid pollution)
/README.md
/LICENSE
/docs/
/examples/

# === OAuth artifact hygiene (prevent recurrence of auth_code leak) ===
**/auth_code.txt
**/auth_url*.txt
**/auth_*.txt

# === Temp / debug-only files that have leaked before ===
projects/daily-mail-brief/temp_brief.md
projects/daily-mail-brief/temp_*.py

# === AI-091 (2026-07-27): personal data untracked under projects/ ===
# Derived personal outputs from ad-hoc analyses (NOT produced by any scheduled
# skill — verified). Files stay on disk; untracked so real financial/health
# values stay out of version control going forward.
projects/credit-card-bills/audit_report.json
projects/credit-card-bills/audit_totals.txt
projects/credit-card-bills/details.txt
projects/credit-card-bills/final_lapse_result.txt
projects/credit-card-bills/json_list.txt
projects/credit-card-bills/artifacts/
# One-off fitness analysis reports with real physiological data. Explicit list,
# not a glob — README.md/CLAUDE.md/REAUTH.md in the same directory stay tracked.
projects/fitness-data-sync/DETAILED_FITNESS_EVOLUTION_REPORT.md
projects/fitness-data-sync/FITNESS_ANALYSIS.md
projects/fitness-data-sync/FITNESS_THESIS_2024_2026.md
projects/fitness-data-sync/FORMAL_PHYSIOLOGICAL_AUDIT_2024_2026.md
projects/fitness-data-sync/MEGA_PERFORMANCE_THESIS_2024_2026.md
projects/fitness-data-sync/PRO_LEVEL_PERFORMANCE_AUDIT.md
projects/fitness-data-sync/THE_EVOLUTION_OF_THE_ENGINE.md
projects/fitness-data-sync/ULTIMATE_PRO_PERFORMANCE_AUDIT_2024_2026.md
projects/fitness-data-sync/ULTRATHINK_CARDIAC_REPORT.md
# One-off scripts whose SOURCE contains hardcoded medical/financial PII
# (unreferenced by any skill — verified). Untracked as an interim measure;
# pending a delete-vs-parameterize decision (see BACKLOG AI-091 notes).
projects/health-reports/scripts/create_clinical_summary_pdf.py
projects/health-reports/scripts/create_fertility_report.py
projects/health-reports/scripts/create_hla_health_report.py
projects/health-reports/scripts/create_knee_recovery_pdf.py
projects/health-reports/scripts/create_report.py
projects/health-reports/scripts/knee_recovery_link.txt
projects/pan-transfer/scripts/create_account_transfer_report.py
# Captured invoice-template dumps (full PAN/address) moved to
# ~/Documents/personal-imports/2026-07-27/ — block re-capture at these paths.
projects/hemir-invoice/template_analysis.txt
projects/hemir-invoice/scripts/template_content_check.txt
# No loose code directly under projects/ — every project is a directory (same
# class of gap as the root /*.py rule; ad-hoc Gmail scripts accumulated here).
projects/*.py

# === User-personalized prompts (do not auto-stage in public clones) ===
# bot-instructions.md is the static system prompt appended to claude/zclaude spawns
# via --append-system-prompt-file. Each user customizes it locally.
# The private repo's existing tracked copy is unaffected — gitignore only
# prevents NEW additions in fresh clones (e.g. of the future public repo).
projects/telegram-bot/bot-instructions.md
