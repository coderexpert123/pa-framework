# Agentic Brain — pa CLI (`pa/`)

Auto-loads on any read under `pa/`; carries pa-internal detail.

- **Locks & state**: `reservations.ts` claims are TTL-based, advisory, per-path; the `@build` reservation is auto-acquired by build/test (build-lock.ts — never claim by hand). The dist-identity guard (`assertDistFresh`) fails closed on stale dist — build before every test (`PA_ALLOW_STALE_DIST=1` is the bisect escape). Blackboard locks are PID+heartbeat with classifyLock eviction (dead→evict, alive+stale→grace that still blocks). Renewal is tri-state (AI-179, 2026-09-03): `renewHeartbeat` returns `updated`/`row-absent`/`write-failed`; a write failure retries on the `PA_HEARTBEAT_WRITE_RETRY_MS` ladder (default 1 s/5 s/15 s) and `startLockRenewal` verifies the row (`peekLockRow`) before firing `onLost('purged')` — a transient EPERM on the atomic-replace rename no longer aborts a healthy run. `pa run` exits non-zero on failure (`exitCodeForCommandResult`: success → 0; `exitCode` 1–255 passthrough; else 1) — chains' `on_failure` now actually fires; the bot's spawns stay fire-and-forget by design.
- **Maintenance runner in-flight guard is per-process; skip:in-flight is unreachable on the pa host (AI-196 closed-by-proof, 2026-09-03):** every pa-host process makes exactly one awaited `runDueJobs` pass — `pa catchup` holds its `catchup` blackboard lock around the call, and `pa maintenance run <job>` takes NO lock but runs one forced pass in its own process — so the per-process `IN_FLIGHT` set is empty at every production decision and the skip can never burn a due-check there. A bounded wait-and-redecide in runner.ts would poll a set that is always empty on that path: dead code, do not add one. Cross-process residual CLOSED 2026-09-04 (AI-200): `pa maintenance run` takes catchup's own pass lock around the forced run and refuses loudly (exit 1, stderr naming the holder) when it is held; `--dry-run` takes no lock (previewJob is read-only). Gotcha: the pass lock is TWO keys, `catchup` + `catchup:topic:default`, never a per-job scheme — catchup topic-suffixes its lock key, and the pass-driving tick is `catchup --topic default` on Windows (live `run-catchup-hidden.vbs`) but bare `catchup` on posix, so locking only bare `catchup` would have been a no-op on this host.
- **Mutate contract** (AI-177 fix): `reservations.mutate`'s fn returns `{result, logEntries}` — decision logs flush only AFTER the store write commits. `renew(id, {session})` is owner-checked. `safeLockOptions({compromisedPolicy})`: 'fail' tags + routes rejection for reservations (never throws from the timer).
- **Test discipline**: scoped runs via `npm test -- <bare-basename>`; the run-tests.mjs dark-file detector fail-closes on zero-test files; inherited `NODE_TEST_CONTEXT` can silently skip all files in spawned runners — NOT stripped by the runners (a test spawning the runner must delete it itself, see runner-env-scrub.test.ts). Both run-tests.mjs wrappers scrub deployment-env flags (`PA_RICH_MESSAGES` etc., list in `src/lib/test-env-scrub.ts`) from the suite spawn — `pa run` injects all secrets.env values into LLM-worker shells, so gate-run tests otherwise inherit live feature flags (AI-199: PA_RICH_MESSAGES=1 rerouted worker replies over /sendRichMessage and failed 12 subtests on every push-gate run). When running suites by hand, leave `PA_TEST_TMP_DIR` unset unless you mean to change the maintenance allowlist (a test pins its `C:/wt/tmp` default), and never point TMP/TEMP at a directory inside a git worktree — walk-up fixtures resolve the worktree's `.git` and their expected rejections go missing.
- **docs-lint trim counter** (`tests/docs-crossref.test.ts`, fix 305246a 2026-09-03): split detection = one memoized `git show --name-status -- '*.md'` probe per trim candidate (`A` status = split → counter reset); `git log --numstat` CANNOT mark new files (a created file reads as `added<TAB>0<TAB>path`, identical to an add-only modify, and the per-file query never lists the new file's row anyway). Commits stream OLDEST-FIRST (`--reverse`) so a split's reset clears only pre-split trims — newest-first leaves them counted when the split is the window's newest trim. Tool-layer gotcha found here: the old regex carried a literal backspace control byte where a `\b` escape was intended (written somewhere in its tool path), making it silently unmatchable — byte-scan source (`ord(c) < 32` sweep) when a "never matches" regex is suspected.
- **exec-fed sensors (c-disk-floor-watchdog, 2026-09-03):** `child_process.exec` routes through cmd.exe — a bare PowerShell expression fails (".Free was unexpected at this time"); wrap it `powershell -NoProfile -Command "..."` (the process-tree.ts:94 convention). And `Get-PSDrive` wants the drive name WITHOUT the colon: `Get-PSDrive C:` matches nothing, exits 0 with EMPTY stdout, and `Number('') === 0` turns that into a sensor reading of "0 bytes" that fires a real alert. Parse exec output strict-digits and embed the raw output in the throw — injected-execFn tests cannot see either class of bug; only the live CLI smoke crossed the seam.
- **public-sync's skip filter is FILE-shaped; tar directory members need their own handling (2026-09-04):** `git archive` emits a DIRECTORY member for every parent of an extracted file, and those members are never in `computeSkippedPaths`' output (check-ignore lists files only) — unhandled, a sync whose file skips all worked still leaves EMPTY directory husks of excluded trees in the mirror (pa-public carried empty `plans/` and `projects/fitness-data-sync/` after 2026-09-04 03:35 IST; the "filter didn't skip" report was these husks — 0 files under them). `extractHead` expands the skip list with slash-less ancestors and the python extractor compares the slash-stripped member name: **python's `tarfile` strips the trailing slash from directory `TarInfo.name` while `tar -t` display keeps it — never copy the display form into a member-name match.** Dir members also never count toward SKIPPED (privateHead counts blobs only). Fixture rule: a boundary fixture that excludes only single files (`*.secret`) can never see this; production's whole-tree patterns (`/*`, `projects/*`) are the shape to replicate.
- **DEFAULT_MODEL_PRICING rows are TIME-BOUND (2026-09-04):** the gemini-3.6/3.7/3.8-flash rates ($0.75/$3.75/$0.075 per 1M) are the official page's through-2026-12-31 prices and DOUBLE on 2027-01-01 — re-check `src/lib/model-pricing.ts` + the three doc/example cost tables ~2026-12-15 (backlog item tracks it; verified against ai.google.dev's page, updated 2026-09-03 UTC).
- **Placement gate (`pa/scripts/placement_gate.py`, 2026-09-04)**: `gen` derives the public-path projection from `inventory/placement-registry.md`; `check` refuses mirror-new files matching no public-eligible row (exit 0/2/3/4 = ok/refusal/stale/usage — argparse's default usage exit 2 is overridden to 4 so it never collides with refusal). Its census parser shares the 12-cell table grammar with `scratch/placement-audit/check_completeness.py` — a registry schema change must move BOTH parsers; the projection's registry sha256 freshness check turns drift into exit 3. The parser reads census-section tables only (anchored on the `HEADER_CELLS` header + heading census token): the Doubtful ask-table's 3-cell rows and the Phase-2 gaps view are skipped, so the ≠12-cells parse error applies to census rows only.
- Per-file detail: `inventory/pa-lib.md`.
- **A16 provenance scrub (2026-09-04)**: all comment/string `plans/...` references under pa/src, pa/tests and pa/scripts are genericized to dated prose ("the 2026-08-23 alerts-wave spec" style), path dropped, zero functional changes. The one functional ref (`postmortem.ts`'s write target into the repo plans index) was ADJUDICATED and RESOLVED same-day (a190375): postmortems write under PA_HOME (`~/.pa/plans/postmortems/` + `~/.pa/plans/INDEX.md`), the `repoRoot` override / `RollbackDeps.postmortemRepoRoot` / wrong-root guard are gone, tests isolate via tmp `PA_HOME`. Remaining residuals, all other waves' property, do NOT "finish" them blindly: types.ts/worker-exec.ts carry the in-flight raw-send wave (which itself ADDS new plans/ refs needing the same scrub when it lands); docs-crossref.test.ts was held by the placement-audit-ce session. Gotcha: the A16 census predates recently landed wave files — 13 hits in 10 files it never listed; any future scan must re-run `git grep` over tracked files, never trust the census as complete.
